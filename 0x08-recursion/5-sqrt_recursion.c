#include "main.h"
/**
 * _sqrt_recursion - fcn that returns the sqrt of a number
 * @n: input
 * Return: int
 */
int _sqrt_recursion(int n)
{
	int k;

	if ((n ^ 0.5) % 1 == 0)
	{
		k = (n ^ 0.5);
	return (k);
	}
	else
		return (-1);

}
